version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: ./app/Dockerfile
    ports:
      - "4000:4000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=production
    command: npm start
